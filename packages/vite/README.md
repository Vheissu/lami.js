# @lami.js/vite

Vite plugin for compiling `.lami.html` templates.

```ts
import { lami } from '@lami.js/vite';

export default {
  plugins: [lami()]
};
```

See [Compiler, SSR, And Hydration](../../docs/compiler-ssr-hydration.md).
